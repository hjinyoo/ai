조건문
if문, switch문, 삼항연산자, 내장객체), ==, ===

switch 조건문
switch(비교할 값) {
case 값: 
	문장; break;
case 값: 
	문장; break;
Default:
	문장; break;
}
switch안에 올 수 있는 값은 수나 문자, boolean 모두가능

삼항연산자
(불 표현식)? (참일 때 실행하는 문장) : (거짓일 때 실행하는 문장);

ajax
jQueryIntro
jQuery
자바 스크립트 라이브러리 중 가장 인기 있는 라이브러리. 이벤트 대응, 애니메이션과 메뉴효과 등에서 가장 단순하고 쉽게 페이지 제작을 할 수 있도록 함
기능 – 문서 객체 모델과 관련된 처리를 쉽게 구현
-일관된 이벤트 연결을 쉽게 구현
-시각적 효과를 쉽게 구현
-aJax 애플리케이션을 쉽게 개발

Query는 적게 쓰고 큰 효과를 내도록 하는 경량화된 자바스크립트 라이브러리
Ajax나 DOM등을 다룰 때 복잡한 일들을 jQuery 사용으로 단순됨

사용법
jQuery.com(http://jquery.com/download)에서 다운로드하거나,구글과 같은 CDN(Content Delivery Network) 으로부터 jQuery를 포함시키는 방법이 있다.(CDN방식)
-	http://code.jquery.com/jquery

	<script	 src="http://code.jquery.com/jquery-1.12.4.js"></script>
	<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>

다운 로드 방식이 아닌 CDN 방식의 경우, 브라우저로 여러 사이트를 돌아다니다 보면 사용자가 CDN 정보가 cash되어 있어 빨리 로드

jQuery 기본 문법
$('선택자').action();
  1) html()  해당 선택자 안에 html코드 삽입 
  2) text()   해당 선택자 안에 text 삽입 
  3) val('변형될 value') ; value값 변형할당
５) 이벤트 관련 함수들
     click(콜백함수), focus(콜백함수), etc...
６) aJax관련 함수들 : $.get(), $.post(), $.ajax()

${‘selector’).action()의 예
웹문서(document)가 준비(onload)될 때(jQuery의 ready이벤트는 onload이벤트이다)
	// $ : jQuery를 의미한다 $(“선택자”).css(“속성”,”속성값”);
	// jQuery 메소드가 여기 온다.
});
이미지가 로드된 후에 사이즈를 변경한다던지, element가 만들어진 이후에 hide되는 등의 에러를 막기 위해 ready이벤트에 씀

keyup() 메소드
키보드가 눌렸다 떨어질때 실행될 함수 등록


Ajax : 서버와 데이터 교환을 하는 기술 
웹페이지의 특정부분(한파트)를 업데이트하고 서버와 데이터를 교환하는 기술
jQuery에서는 Ajax와 관련하여 몇 개의 메소드를 제공
jQuery Ajax메소드를 사용하게 되면 외부데이터를 로딩해서 보여질 수 있음
jQuery를 이용하지 않으면 Ajax이용이 어려울 수 있으나 jQuery를 이용하면 쉬운 코딩


$.ajax({/*	$.ajax(json형식으로 option 설정)
		ex) $.ajax({url:'', type:'get', data:~ , success:function(data,status){} });
url : 요청 자원(서버)를 호출, 일반적으로 호출된 자원에서 json형식으로 데이터 return 
type : 요청처리방식
data: 요청시, 함께 전달되는 데이터 
dataType : 요청 후, 응답의 결과로 반환되는 데이터의 종류를 선언
ex. Xml, html, joson, text
success : 성공했을 때, 처리할 내용, 콜백함수로 나타냄
ex. Function(data, status){}
error : 응답이 에러가 났을 때, 에러 상태에 대한 코드를 반환
		ex. error :  function(code){
		  		code:에러에 대한 code 내용 전달...
}
*/})

ajax_get
$.get(요청경로, callback(data, status){
data : 요청경로 url을 수행결과
status : success/fail

fetch()
자바스크립트에서 네트워크 요청을 보내고 응답을 받아오는데 사용되는 최신 웹 API
XHR을 대체함
호출하면 즉시 Promise 객체가 반환
이 Promise를 사용하면 .then()과 .catch()를 연결하여 비동기 작업의 성공(응답 수신)과 실패(네트워크 오류)를 깔끔하게 처리 가능



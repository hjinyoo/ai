반복문
배열에서의 반복문과 forEach함수, do~while문, while문

while(조건식){
조건식이 참일 때 계속 실행할 명령문들;
}

do{
최초 한번은 무조건 실행. 두번째부터는 조건식이 참이면 계속 수행할 명령문들;
} while(조건식);

for( ①초기값 ; ②반복할조건 ; ④증감식){
③ 반복할 명령문들;
}

for(var idx in array){   }
for(var data of array){  }

배열변수.forEach(function(data, index){
        첫번째 인덱스부터 끝 인덱스까지 반복할 명령문들;
});


배열
※ 어떤 자료형도 배열 요소가 될 수 있다

변수명.pop();  // 제일 끝방의 값을 return하고 없앰
※배열의 length속성과 push()메서드

'length of array : ' + array.length); 

for-in문/for-of문 : object나 array에서 사용)

for-Each함수 : array만 사용       
 arr.forEach(function(data,idx)
for-Each함수에서 index가 필요없을 때
arr.forEach(function(data){
document.writeln(data +'<br>');


while 반복문